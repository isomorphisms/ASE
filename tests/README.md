# Tests

This directory is only a place to keep occasional before/after checks of whether the automotive corpus improves an assistant's answers.

It is not a training gym, benchmark suite, or exam-prep project.

Keep `tests/` out of retrieval. Add a test only when there is a concrete reason to preserve one.
